int x = -0;                         // Testa -0
int v = -1;                         // Testa Número Negativo
bool _flag = false;                 
if (x >= 5 && _flag) {
    print(x);
    int 1abc;                       // Erro Léxico: ID inválido
} else {
    read(y);
    z1 = x + 20;
    int @abc;                       // Testa simbolo não idenficado
}
while (_flag || x < 0) {
    x = x - 1;
}
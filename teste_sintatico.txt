int x = ;       // Erro 1: Esperava expressão depois de '='
bool z = /;
if (x > 5) {       // Erro 2: Faltando ')'
    print(a);

x = 5 + * 2;    // Erro 3: Operador '*' inesperado

print(x         // Erro 4: Faltando ')'
;               // O parser vai se recuperar neste ';'
int a = 1;      // Um statement válido para continuar

else { }        // Erro 5: 'else' sem 'if'. O parser vai se recuperar no '}'

while (x) {     // Erro 6: '}' faltando no final do arquivo
    x = x - 1;
}
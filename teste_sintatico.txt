int 1q = -0;        // Erro Léxico: ID começando com numero
int x = ;           // Erro Sintático: Esperava expressão depois de '='
bool z = /;         // Erro Sintático: Não deveria ter '/' atribuido
bool z = true;
if (x > 5 {         // Erro Sintático: Faltando ')'
    print(a);

    int @a = 1;     // Erro Léxico: Caractere especial inesperado

x = 5 + * 2;    // Erro Sintático: Operador inesperado

print(x             // Erro Sintático: Faltando ')'
;                   // O parser vai se recuperar neste ';', /*
                    //assim é necessário olhar para trás do ';' para enxergar o erro

else { }            // Erro Sintático: 'else' sem 'if'. O parser vai se recuperar no '}'

while (x) {         // Erro Sintático: '}' faltando no final do arquivo,
    x = x - 1;}     // mas que não consigo identificar ou tratar da maneira correta ao indificar o que falta
                    // Ele até conta colunas de comentários de tão potente que é
